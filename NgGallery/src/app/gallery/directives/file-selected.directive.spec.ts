import { FileSelectedDirective } from './file-selected.directive';

describe('FileSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new FileSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
