import { ControlAlertDirective } from './control-alert.directive';

describe('ControlAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlAlertDirective();
    expect(directive).toBeTruthy();
  });
});
